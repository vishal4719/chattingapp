# Required — backend API URL (no trailing slash)
VITE_API_URL=http://localhost:3000

# Required — WebSocket URL (same as API when using server.ts locally)
VITE_WS_URL=http://localhost:3000

# Optional — poll for messages when WebSocket unavailable (ms, default 3000)
# VITE_POLL_INTERVAL_MS=3000
