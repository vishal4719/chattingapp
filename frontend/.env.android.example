# Optional — only if you switch capacitor.config.ts to bundle assets locally
# instead of loading https://chatapp.vishaltech.in (not the default).

VITE_API_URL=https://api.vishaltech.in
VITE_WS_URL=https://api.vishaltech.in
